<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.movieAndgame.Dao.MovieReviewDaoImpl">

	<insert id="reviewSave" parameterType="com.movieAndgame.Dto.GameReviewDto">
		
		select into game_review(writer, title, game_name, target_post, write_date)
			values (#{writer}, #{title}, #{game_name}, #{target_post}, now())
		
	</insert>

</mapper>